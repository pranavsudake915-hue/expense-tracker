# expense-tracker
A responsive Expense Tracker built with React that lets users add, delete, and categorize expenses while automatically calculating totals. Includes real-time currency conversion using a public exchange rate API.
